using System.Net;
using System.Text.Json;


namespace Helpers{

    public class ApiHelper{

        // public static async Task<string> GetWithCookieHeaderAsync(string url,string value,string name="token")
        // {
        //     using var httpClient = new HttpClient();
            
        //     // Set cookies manually in headers
        //     httpClient.DefaultRequestHeaders.Add("Cookie",new Cookie(name,value));

        //     var response = await httpClient.GetAsync(url);
        //     response.EnsureSuccessStatusCode();
        //     var responseBody = await response.Content.ReadAsStringAsync();
        //     Console.WriteLine($"{responseBody}\n");
            
        //     return responseBody;
        // }
    }
}